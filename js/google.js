const searchInput= document.querySelector("#Search_window");
const search_icon=document.querySelector("#Search_btn");

search_icon.addEventListener("submit",SubmitHandler);
function SubmitHandler(){
    searchInput.sub
}